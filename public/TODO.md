Still need to ammend the db to allow searching of users via view of users table.
Implement currencies system
allow for user creation of different categories and to remove some (if there are transactions that exist with them from before deletion they keep this for views and such)
Allow users to edit or ammend an entry
Set a budget
Set recurring charges
Fix Light mode background
auto update on the balance adjustment table same as recent transactions
left nav collapse button needs to be moved to the top right of the left nav
left nav needs a burger menu icon when collapsed.
Check readability on mobile
Allow users to implement there own specified accounts.
make the add view work better
allow users to set automatic income tax percentage in the settings
CHAT ROOM?
import via csv support?
Add more functionality in settings menu 